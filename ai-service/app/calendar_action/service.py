SWCAMP 23기_김정훈
kjh_0_
자리 비움

노랭이 — 오전 12:03
일단 반영해서 낼 피드백 한번 더 해주세요
달님반-박세민 — 오전 12:03
이건 진짜 후순위 인데 시간 나면  택스트랑 정리좀 ......
노랭이 — 오전 12:03
깔아놓고 추가하는건 금방이니까
네네
SWCAMP 23기_김정훈 — 오전 12:04
dhkdn
와우
시드 추가 지금 actions 돌렸음
프론트 는 이제 올려도 되고
달님반-박세민 — 오전 12:05
각 설명들인데 헤헤
이미지
고생하셨슴다 형님!
지.. 지..여나!1..
미안해 못난 팀장이라
ㅜ.ㅜ
노랭이 — 오전 12:05
오 대박
조아요
ppt 먼저 하고 곧바로 해보겟습니당
달님반-박세민 — 오전 12:06
얍
형 저희 그럼 최종 발표는 어떤 회사로 진행 할까요 ?
SWCAMP 23기_김정훈 — 오전 12:16
데모 회사 1 — 서울디지털테크
이메일이름팀역할admin1@workforce.com정인사(관리자)시스템 관리자emp_demo1_1@gmail.com정수영인사팀팀장emp_demo1_2@gmail.com한지호인사팀인사 관리자emp_demo1_3@gmail.com오미경인사팀인사팀원emp_demo1_4@gmail.com강대훈인사팀인사팀원emp_demo1_5@gmail.com윤재성인사팀인사팀원emp_demo1_6@gmail.com박서연인사팀인사팀원emp_demo1_7@gmail.com이준혁개발팀팀장emp_demo1_8@gmail.com최유진개발팀일반 직원emp_demo1_9@gmail.com임도현개발팀일반 직원emp_demo1_10@gmail.com조성훈개발팀일반 직원emp_demo1_11@gmail.com황민지개발팀일반 직원emp_demo1_12@gmail.com권시우개발팀일반 직원emp_demo1_13@gmail.com송하늘기획팀팀장emp_demo1_14@gmail.com안재현기획팀일반 직원emp_demo1_15@gmail.com노유리기획팀일반 직원emp_demo1_16@gmail.com유태민기획팀일반 직원emp_demo1_17@gmail.com백서영기획팀일반 직원emp_demo1_18@gmail.com문지원기획팀일반 직원
데모 회사 2 — 강남솔루션 (admin2)
같은 이름/팀/역할 구성. 이메일만 emp_demo2_1 ~ emp_demo2_18@gmail.com
데모 회사 3 — 판교크리에이션 (admin3)
같은 이름/팀/역할. 이메일만 emp_demo3_1 ~ emp_demo3_18@gmail.com
엑
음 지금 맴버가 말썽이군
달님반-박세민 — 오전 12:20
헉
SWCAMP 23기_김정훈 — 오전 12:22
혹시 role에 인사 관리자 인사팀원 일반 직원 이렇게 가는 거맞지??
(시스템 관리자→관리자, 인사 관리자→인사 팀장, 인사팀원→인사 팀원, 일반 직원→직원) ?
달님반-박세민 — 오전 12:22
넵 맞아요
인사 팀장과 인사 팀원의 차이는 목표 평가 권한 차이정도 입니당
SWCAMP 23기_김정훈 — 오전 12:23
그러면 인사 관리자란 말이 시스템 관리자 느낌이 있어서 팀장으로 간걸로
달님반-박세민 — 오전 12:24
네넵 그래서

관리자 , 인사팀장, 인사  팀원,  팀장, 직원

이렇게 4개가 회사 온보딩시 기본 생성 롤입니당
https://www.youtube.com/@PlaydataEncore/videos

여기 옜 기수들 발표 한 건데 참고 할 거 있으면 참고해도 괜찮을듯 지조리카 @노랭이
YouTube
플레이데이터 PLAYDATA
데이터 전문 기업 (주)엔코아의 교육 브랜드, 플레이데이터!

플레이데이터는 대한민국 청년들이 원하는 꿈을 더 정확하고 빠르게 이룰 수 있도록,
전문적이고 체계적으로 개발자 취업의 바른 길로 안내합니다.

플레이데이터 바로가기
https://bit.ly/2V9eGUd
이미지
노랭이 — 오전 12:27
오 네 감사합니다
노랭이 — 오전 2:58
dhQK
오빠
ppt 어떤 부분 필요하세요??
위에 다은이가 했던 부ㅜㅂㄴ??
SWCAMP 23기_김정훈 — 오전 2:58
전체 ?
아니면
그
노랭이 — 오전 2:58
아님 제가 새로 하는 부분??
SWCAMP 23기_김정훈 — 오전 2:58
핵심서비스
노랭이 — 오전 2:59
아아
다은이가 해놓은 부분
잠시만여
SWCAMP 23기_김정훈 — 오전 2:59
3장만 줘도 되겠다
노랭이 — 오전 2:59
이미지
이거 세개 말하시는ㄱ죠
SWCAMP 23기_김정훈 — 오전 2:59
엉
노랭이 — 오전 3:00
이미지
이미지
이미지
pdf로 드릴까요??
SWCAMP 23기_김정훈 — 오전 3:00
와근데 ppt로 받을려고했어
노랭이 — 오전 3:00
앗 글쿠나
ppt 파일로요??
SWCAMP 23기_김정훈 — 오전 3:00
아아니다 이거면
될거같애
땡큐
노랭이 — 오전 3:01
첨부 파일 형식: document
핵심만.pptx
4.08 MB
노랭이 — 오전 9:11
https://www.figma.com/proto/FftNc7FU8cuQZS2VnKm3NR/be23-fin-team4-workforce?node-id=157-26483&viewport=131%2C993%2C0.21&t=x91eXCFunnOs1mwu-1&scaling=min-zoom&content-scaling=fixed&starting-point-node-id=157%3A26483&show-proto-sidebar=1&page-id=0%3A1
Figma
be23-fin-team4-workforce
Created with Figma
이미지
노랭이 — 오전 9:19
이미지
노랭이 — 오전 11:43
https://www.notion.so/jiyean/BEYOND-SW-35e00cd484e080e09f4fc2e15986fd3b?source=copy_link
jiyean on Notion
BEYOND SW 캠프 최종프로젝트 | Notion
WORKFORCE / 4팀(박세민 조장) / 주제 : 효과적인 HR을 위한 인사 관리 시스템 / 직관적인 사용자 경험과 챗봇 기반 안내로, 누구나 쉽게 적응하고 빠르게 업무에 몰입할 수 있도록 지원하는 HR 시스템
BEYOND SW 캠프 최종프로젝트 | Notion
다은 — 오후 6:40
불친절한 UI로 인해 저빈도 사용자가 시스템 대신 인사팀 직접 문의 선택

연차 잔여분, 급여 내역 확인 등 단순 정보 문의의 인사팀 쏠림 현상

단순 응대 업무의 누적으로 인한 인사팀의 본연 업무 차질
노랭이 — 오후 7:13
https://workforce-profiles.s3.ap-northeast-2.amazonaws.com/mc/744751d9-aa39-43e5-8864-9f17d22dfbe0/1778580774261_b192b92a48dd4c5299fb1c7fdc6cb9d8.png?X-Amz-Algorithm=AWS4-HMAC-SHA256&X-Amz-Date=20260512T101254Z&X-Amz-SignedHeaders=host&X-Amz-Credential=AKIA2NUNT5NTESSBPBNO%2F20260512%2Fap-northeast-2%2Fs3%2Faws4_request&X-Amz-Expires=3600&X-Amz-Signature=080aee4199abf7b592a27416c5d3438b8078f5d26068415b0b0b823737bf986c
달님반-박세민 — 오후 7:54
"""챗봇 캘린더 액션 메인 로직"""
import json
import logging
from typing import Optional
from datetime import date, datetime, timedelta, timezone

from app.calendar_action import calendar_client, state
from app.calendar_action.schema import (
    CalendarActionRequest,
    CalendarActionResponse,
    CalendarActionState,
    CalendarActionButton
)
from app.core.openai import client as openai_client

logger = logging.getLogger(__name__)

# KST 타임존 (컨테이너가 UTC라 명시 필요)
KST = timezone(timedelta(hours=9))


# ============================================
# Function Calling Tool 정의 (고정)
# ============================================

CALENDAR_TOOL = {
    "type": "function",
    "function": {
        "name": "create_calendar_event",
        "description": "사용자가 요청한 캘린더 일정 정보를 추출합니다.",
        "parameters": {
            "type": "object",
            "properties": {
                "title": {
                    "type": "string",
                    "description": "일정 제목"
                },
                "description": {
                    "type": "string",
                    "description": "일정 상세 설명 (선택)"
                },
                "startAt": {
                    "type": "string",
                    "description": "시작일시 (YYYY-MM-DDTHH:mm:ss 형식)"
                },
                "endAt": {
                    "type": "string",
                    "description": "종료일시 (YYYY-MM-DDTHH:mm:ss 형식)"
                }
            }
        }
    }
}

REQUIRED_FIELDS = [
    {"name": "title", "label": "일정 제목"},
    {"name": "startAt", "label": "시작일시"},
    {"name": "endAt", "label": "종료일시"}
]


# ============================================
# 진입점
# ============================================

async def handle_calendar_action(
        request: CalendarActionRequest,
        company_id: str,
        member_id: str,
        member_position_id: str,
        authorization: str) -> CalendarActionResponse:
    """캘린더 액션 메인 핸들러"""
    logger.info(f"[calendar_action] ========================================")
    logger.info(
        f"[calendar_action] question='{request.question}', "
        f"session_id={request.session_id}, action={request.action}"
    )
    # 0. 액션 진행 중 사용자가 텍스트로 취소 의도 입력
    if request.session_id and request.question and not request.action:
        if _is_cancel_intent(request.question):
            logger.info(f"[calendar_action] 취소 키워드 감지로 세션 종료")
            state.clear_state(request.session_id)
            return CalendarActionResponse(
                type="cancelled",
                message="일정 등록을 취소했습니다. 새로운 질문을 입력해주세요.",
                session_id=None
            )

    # 1. 사용자가 버튼 누른 경우
    if request.action and request.session_id:
        return await _handle_user_action(
            request, company_id, member_id, authorization
        )

    # 2. 신규 또는 진행 중 세션
    current_state = _load_or_create_state(
        request.session_id, company_id, member_id, member_position_id
    )

    # 3. 슬롯 추출
... (376줄 남음)

message.txt
17KB
노랭이 — 오후 8:21
단단
들어왓어요
ㅋㅋㅋㅋㅋㅋ
﻿
"""챗봇 캘린더 액션 메인 로직"""
import json
import logging
from typing import Optional
from datetime import date, datetime, timedelta, timezone

from app.calendar_action import calendar_client, state
from app.calendar_action.schema import (
    CalendarActionRequest,
    CalendarActionResponse,
    CalendarActionState,
    CalendarActionButton
)
from app.core.openai import client as openai_client

logger = logging.getLogger(__name__)

# KST 타임존 (컨테이너가 UTC라 명시 필요)
KST = timezone(timedelta(hours=9))


# ============================================
# Function Calling Tool 정의 (고정)
# ============================================

CALENDAR_TOOL = {
    "type": "function",
    "function": {
        "name": "create_calendar_event",
        "description": "사용자가 요청한 캘린더 일정 정보를 추출합니다.",
        "parameters": {
            "type": "object",
            "properties": {
                "title": {
                    "type": "string",
                    "description": "일정 제목"
                },
                "description": {
                    "type": "string",
                    "description": "일정 상세 설명 (선택)"
                },
                "startAt": {
                    "type": "string",
                    "description": "시작일시 (YYYY-MM-DDTHH:mm:ss 형식)"
                },
                "endAt": {
                    "type": "string",
                    "description": "종료일시 (YYYY-MM-DDTHH:mm:ss 형식)"
                }
            }
        }
    }
}

REQUIRED_FIELDS = [
    {"name": "title", "label": "일정 제목"},
    {"name": "startAt", "label": "시작일시"},
    {"name": "endAt", "label": "종료일시"}
]


# ============================================
# 진입점
# ============================================

async def handle_calendar_action(
        request: CalendarActionRequest,
        company_id: str,
        member_id: str,
        member_position_id: str,
        authorization: str) -> CalendarActionResponse:
    """캘린더 액션 메인 핸들러"""
    logger.info(f"[calendar_action] ========================================")
    logger.info(
        f"[calendar_action] question='{request.question}', "
        f"session_id={request.session_id}, action={request.action}"
    )
    # 0. 액션 진행 중 사용자가 텍스트로 취소 의도 입력
    if request.session_id and request.question and not request.action:
        if _is_cancel_intent(request.question):
            logger.info(f"[calendar_action] 취소 키워드 감지로 세션 종료")
            state.clear_state(request.session_id)
            return CalendarActionResponse(
                type="cancelled",
                message="일정 등록을 취소했습니다. 새로운 질문을 입력해주세요.",
                session_id=None
            )

    # 1. 사용자가 버튼 누른 경우
    if request.action and request.session_id:
        return await _handle_user_action(
            request, company_id, member_id, authorization
        )

    # 2. 신규 또는 진행 중 세션
    current_state = _load_or_create_state(
        request.session_id, company_id, member_id, member_position_id
    )

    # 3. 슬롯 추출
    extracted = await _extract_slots(
        question=request.question,
        history=request.conversation_history,
        existing_slots=current_state.slots
    )
    if extracted:
        # 이미 채워진 슬롯은 보존 (LLM이 generic 값으로 덮어쓰는 것 방지)
        for key, value in extracted.items():
            if not value:
                continue
            existing = current_state.slots.get(key)
            # title/description은 이미 있으면 그대로 유지
            if key in ("title", "description") and existing:
                continue
            current_state.slots[key] = value

    # 4. 부족한 필드 확인
    missing = _find_missing_fields(current_state.slots)

    if missing:
        state.save_state(current_state)
        return _ask_response(current_state, missing)

    # 5. 모든 슬롯 채워짐 → 확인 단계
    state.save_state(current_state)
    return _confirm_response(current_state)


# ============================================
# 사용자 액션 처리
# ============================================

async def _handle_user_action(
        request: CalendarActionRequest,
        company_id: str,
        member_id: str,
        authorization: str) -> CalendarActionResponse:
    """버튼 클릭 처리"""
    current_state = state.load_state(request.session_id)

    if not current_state:
        return _error_response("세션이 만료되었습니다. 다시 시작해주세요.", None)

    # 보안 검증
    if current_state.member_id != member_id:
        logger.warning(
            f"[calendar_action] 세션 도용 시도: "
            f"owner={current_state.member_id}, requester={member_id}"
        )
        return _error_response("세션에 접근할 권한이 없습니다.", None)

    action = request.action

    if action == "create_event":
        return await _create_event(current_state, authorization)

    if action == "cancel":
        state.clear_state(request.session_id)
        return CalendarActionResponse(
            type="cancelled",
            message="일정 등록이 취소되었습니다.",
            session_id=request.session_id
        )

    return _error_response(f"알 수 없는 액션: {action}", request.session_id)


# ============================================
# 일정 생성
# ============================================

async def _create_event(
        current_state: CalendarActionState,
        authorization: str) -> CalendarActionResponse:
    """개인 일정 즉시 생성"""
    payload = {
        "title": current_state.slots.get("title"),
        "description": current_state.slots.get("description", ""),
        "startAt": current_state.slots.get("startAt"),
        "endAt": current_state.slots.get("endAt"),
        "eventType": "PERSONAL",
        "isPublicYn": "YES"
    }

    result = await calendar_client.create_personal_event(
        current_state.company_id,
        current_state.member_id,
        payload,
        authorization
    )

    if not result:
        return _error_response(
            "일정 등록에 실패했습니다. 다시 시도해주세요.",
            current_state.session_id
        )

    state.clear_state(current_state.session_id)

    title = current_state.slots.get("title")
    start_at = current_state.slots.get("startAt", "")
    end_at = current_state.slots.get("endAt", "")

    formatted_start = _format_datetime(start_at)
    formatted_end = _format_datetime(end_at)

    message = (
        f"일정이 등록되었습니다.\n\n"
        f"- 제목: {title}\n"
        f"- 일시: {formatted_start} ~ {formatted_end}"
    )

    return CalendarActionResponse(
        type="created",
        message=message,
        event_id=result.get("eventId"),
        redirect_url="/app/calendar"
    )


# ============================================
# 슬롯 추출
# ============================================

async def _extract_slots(
        question: str,
        history: str,
        existing_slots: dict) -> dict:
    """LLM Function Calling으로 일정 정보 추출"""
    # KST 기준 날짜 계산
    today = datetime.now(KST).date()
    tomorrow = today + timedelta(days=1)
    day_after = today + timedelta(days=2)

    # 이번주/다음주 요일별 날짜 (월=0 ~ 일=6)
    this_monday = today - timedelta(days=today.weekday())
    next_monday = this_monday + timedelta(days=7)

    weekday_names = ["월요일", "화요일", "수요일", "목요일", "금요일", "토요일", "일요일"]
    this_week_lines = "\n".join([
        f"  - 이번주 {weekday_names[i]}: {(this_monday + timedelta(days=i)).isoformat()}"
        for i in range(7)
    ])
    next_week_lines = "\n".join([
        f"  - 다음주 {weekday_names[i]}: {(next_monday + timedelta(days=i)).isoformat()}"
        for i in range(7)
    ])

    system_prompt = f"""당신은 캘린더 일정 작성 어시스턴트입니다.

[현재 날짜 정보 - KST 기준]
- 오늘: {today.isoformat()} ({_get_weekday_korean(today)})
- 내일: {tomorrow.isoformat()}
- 모레: {day_after.isoformat()}

[이번 주 요일별 날짜]
{this_week_lines}

[다음 주 요일별 날짜]
{next_week_lines}

[이미 입력된 정보]
{json.dumps(existing_slots, ensure_ascii=False) if existing_slots else "(없음)"}

[절대 규칙]
- 사용자 입력에 명시된 정보만 추출하세요. 추측 금지.
- 날짜는 반드시 위의 [이번 주 / 다음 주 요일별 날짜] 표를 그대로 사용하세요.
  - "내일" → {tomorrow.isoformat()}
  - "모레" → {day_after.isoformat()}
  - "다음주 화요일", "이번주 금요일" 등은 위 표에서 그대로 복사하세요. 계산하지 마세요.
- 시간이 명시되지 않으면 startAt/endAt을 비워두세요. 임의로 09:00 등을 넣지 마세요.
- "오후 2시" → 14:00, "오전 10시" → 10:00, "정오" → 12:00, "자정" → 00:00
- 출력 형식: YYYY-MM-DDTHH:mm:ss (예: 2026-05-19T14:00:00)

[자동 생성 허용]
- title: 일정 내용에 어울리는 제목 자동 생성 (단, [이미 입력된 정보]에 title이 있으면 그대로 유지하고 절대 변경하지 마세요)
- endAt: startAt 있고 endAt 없으면 startAt + 1시간 자동 설정 가능

[이전 슬롯 보존 규칙]
[이미 입력된 정보]에 어떤 필드가 있으면 그 필드는 그대로 두세요.
사용자가 명시적으로 변경을 요청하지 않았다면 새 값으로 덮어쓰지 마세요.

예시:
[이미 입력된 정보] {{ "title": "회의" }}
입력: "내일 오후 2시부터 3시까지"
올바름: {{ "title": "회의", "startAt": "{tomorrow.isoformat()}T14:00:00", "endAt": "{tomorrow.isoformat()}T15:00:00" }} (title 유지)
잘못: {{ "title": "일정", "startAt": "...", "endAt": "..." }} (title 변경 X)

[잘못된 추출 예시]
입력: "회의 잡아줘"
잘못: {{ "title": "회의", "startAt": "{today.isoformat()}T09:00:00", "endAt": "{today.isoformat()}T10:00:00" }}
올바름: {{ "title": "회의" }} (날짜·시간 명시 없음)

입력: "내일 회의"
잘못: {{ "title": "회의", "startAt": "{tomorrow.isoformat()}T09:00:00" }}
올바름: {{ "title": "회의" }} (시간 미명시)

입력: "내일 오후 2시 회의"
올바름: {{ "title": "회의", "startAt": "{tomorrow.isoformat()}T14:00:00", "endAt": "{tomorrow.isoformat()}T15:00:00" }}
"""

    messages = [{"role": "system", "content": system_prompt}]
    if history:
        messages.append({"role": "user", "content": f"[이전 대화]\n{history}"})
    messages.append({"role": "user", "content": f"[현재 입력]\n{question}"})

    try:
        response = openai_client.chat.completions.create(
            model="gpt-4o-mini",
            messages=messages,
            tools=[CALENDAR_TOOL],
            tool_choice="auto",
            temperature=0.0
        )

        message = response.choices[0].message
        if not message.tool_calls:
            return {}

        tool_call = message.tool_calls[0]
        arguments = json.loads(tool_call.function.arguments)
        cleaned = {k: v for k, v in arguments.items() if v is not None and v != ""}
        logger.info(f"[calendar_action] 추출된 슬롯: {cleaned}")
        return cleaned

    except Exception as e:
        logger.error(f"[calendar_action] 슬롯 추출 실패: {e}")
        return {}


# ============================================
# 헬퍼 함수
# ============================================

def _find_missing_fields(slots: dict) -> list:
    """필수 필드 중 빠진 것 반환"""
    missing = []
    for field in REQUIRED_FIELDS:
        value = slots.get(field["name"])
        if value is None or value == "":
            missing.append(field)
    return missing


def _load_or_create_state(
        session_id: Optional[str],
        company_id: str,
        member_id: str,
        member_position_id: str) -> CalendarActionState:
    """기존 세션 로드 or 신규 생성"""
    if session_id:
        existing = state.load_state(session_id)
        if existing:
            if existing.member_id != member_id:
                logger.warning(
                    f"[calendar_action] 세션 도용 시도: "
                    f"owner={existing.member_id}, requester={member_id}"
                )
                new_session_id = state.create_session_id()
                return CalendarActionState(
                    session_id=new_session_id,
                    company_id=company_id,
                    member_id=member_id,
                    member_position_id=member_position_id,
                    slots={}
                )
            return existing

    new_session_id = state.create_session_id()
    return CalendarActionState(
        session_id=new_session_id,
        company_id=company_id,
        member_id=member_id,
        member_position_id=member_position_id,
        slots={}
    )


def _ask_response(
        current_state: CalendarActionState,
        missing: list) -> CalendarActionResponse:
    """추가 정보 요청"""
    if len(missing) == 1:
        message = f"{missing[0]['label']}을(를) 알려주세요."
    else:
        labels = [m["label"] for m in missing]
        message = f"다음 정보를 알려주세요:\n- " + "\n- ".join(labels)

    message += "\n\n(다른 질문을 하시려면 \"취소\"라고 입력해주세요)"

    return CalendarActionResponse(
        type="ask",
        message=message,
        session_id=current_state.session_id
    )


def _confirm_response(
        current_state: CalendarActionState) -> CalendarActionResponse:
    """등록 확인"""
    title = current_state.slots.get("title", "")
    start_at = current_state.slots.get("startAt", "")
    end_at = current_state.slots.get("endAt", "")
    description = current_state.slots.get("description", "")

    formatted_start = _format_datetime(start_at)
    formatted_end = _format_datetime(end_at)

    preview_lines = [
        f"- 제목: {title}",
        f"- 시작일시: {formatted_start}",
        f"- 종료일시: {formatted_end}"
    ]
    if description:
        preview_lines.append(f"- 설명: {description}")

    message = (
            f"다음 내용으로 일정을 등록합니다.\n\n"
            + "\n".join(preview_lines)
            + "\n\n등록하시겠습니까?"
    )

    return CalendarActionResponse(
        type="confirm",
        message=message,
        session_id=current_state.session_id,
        preview={
            "title": title,
            "startAt": start_at,
            "endAt": end_at,
            "description": description
        },
        actions=[
            CalendarActionButton(label="등록", value="create_event"),
            CalendarActionButton(label="취소", value="cancel")
        ]
    )


def _error_response(
        message: str,
        session_id: Optional[str]) -> CalendarActionResponse:
    return CalendarActionResponse(
        type="error",
        message=message,
        session_id=session_id,
        error_code="GENERIC_ERROR"
    )


def _format_datetime(iso_str: str) -> str:
    """ISO 형식을 사람이 읽기 좋게 변환"""
    if not iso_str:
        return ""
    try:
        dt = datetime.fromisoformat(iso_str)
        weekday = _get_weekday_korean(dt.date())
        return f"{dt.strftime('%Y-%m-%d')} ({weekday}) {dt.strftime('%H:%M')}"
    except Exception:
        return iso_str


def _get_weekday_korean(target_date: date) -> str:
    weekdays = ["월요일", "화요일", "수요일", "목요일", "금요일", "토요일", "일요일"]
    return weekdays[target_date.weekday()]


def _is_cancel_intent(question: str) -> bool:
    """사용자 입력이 취소/중단 의도인지 감지"""
    cancel_keywords = [
        "취소", "그만", "안할래", "안할게", "관둬", "중단",
        "됐어", "필요없어", "그만할래", "스톱", "stop",
        "다른 질문", "다른거", "다른 거"
    ]
    question_lower = question.strip().lower()
    return any(kw in question_lower for kw in cancel_keywords)